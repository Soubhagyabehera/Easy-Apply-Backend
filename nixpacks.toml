[phases.setup]
nixPkgs = ['python312', 'gcc', 'pkg-config', "libgl1", "libglib2.0-0"]

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build completed"']

[start]
cmd = 'uvicorn app.main:app --host 0.0.0.0 --port $PORT'
